<p><%= t("devise.passwords.email.greeting", email: @resource.email) %></p>

<p><%= t("devise.passwords.email.instructions") %></p>

<p><%= link_to t("devise.passwords.email.change_password"), edit_password_url(@resource, reset_password_token: @token, locale: I18n.locale) %></p>

<p><%= t("devise.passwords.email.ignore") %></p>
<p><%= t("devise.passwords.email.unchanged") %></p>
