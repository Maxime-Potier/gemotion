<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1><%= t("invitation_mailer.heading") %></h1>
    <p><%= t("invitation_mailer.instructions") %></p>
    <%= link_to t("invitation_mailer.join"), @url %>

    <p><%= t("invitation_mailer.signature") %></p>
  </body>
</html>
